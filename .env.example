# Copy to .env.local for `vercel dev`, and add the same keys in
# Vercel → Project → Settings → Environment Variables for production.

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
MAIL_TO=info@mkhwerk.hu

# Optional. Until a domain is verified in Resend, leave this unset and Resend's
# shared sender is used (that one can only deliver to your own Resend account email).
# MAIL_FROM=MKH Werk <ajanlat@mkhwerk.hu>
